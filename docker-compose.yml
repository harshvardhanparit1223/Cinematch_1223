   version: '3.8'
   services:
     app:
       build: .
       ports:
         - "8000:8000"
         - "8501:8501"
       environment:
         - TMDB_API_KEY=${TMDB_API_KEY}
       restart: always
